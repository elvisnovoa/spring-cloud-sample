# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/discovery/eureka/
  instance:
    instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}

# HTTP Server
server:
  port: 8080   # HTTP port for running in standalone mode
  context-path: /api
  
---

spring:
    profiles: tomcat 
    
# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/discovery/eureka/
  instance:
    instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  